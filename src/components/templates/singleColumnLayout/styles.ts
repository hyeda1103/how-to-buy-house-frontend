import styled from 'styled-components';

export const Container = styled.div`
  position: relative;
  width: 100%;
`;

export const Inner = styled.main`
  position: relative;
  width: 960px;
  min-height: calc(100vh - 176px * 2);
  margin: 0 auto;
  padding: 176px 0;
`;
