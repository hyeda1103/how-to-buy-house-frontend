import styled from 'styled-components';

export const Container = styled.div`
  position: relative;
  width: 100%;
`;

export const Inner = styled.main`
  position: relative;
  width: 960px;
  height: 100%;
  margin: 0 auto;
  padding: 203px 0;
`;
